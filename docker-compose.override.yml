services:
  bag-backend:
    ports:
      - "4001:4001"
    networks:
      - default

  bag-frontend:
    ports:
      - "8080:80" 
    networks:
      - default
    depends_on:
      - bag-backend

networks:
  proxy-network:
    external: false